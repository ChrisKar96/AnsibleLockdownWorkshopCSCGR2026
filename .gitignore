# Goss compliance reports fetched from targets
reports/**/*.json

# Ansible retry files
*.retry

# Roles installed via ansible-galaxy — not tracked in this repo
# Install with: ansible-galaxy install -r requirements.yml -p roles/
roles/*/

.vscode

# Per-host ansible user credentials generated by bootstrap.yml
# These are plaintext password files — never commit them.
credentials/
